version: "2"
services:
  app:
    image: odoo:18
    depends_on:
      - db
    restart: always
    tty: true
    ports:
      - "8069:8069"
    environment:
      - HOST=db
      - USER=odoo
      - PASSWORD=odoo
    volumes:
      - odoo-web-data18:/var/lib/odoo
#      - ./odoo.conf:/etc/odoo/odoo.conf
      - ./payment_twikey:./addons/payment_twikey
  db:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_PASSWORD=odoo
      - POSTGRES_USER=odoo
    ports:
      - 6432:5432
volumes:
  odoo-web-data18:
